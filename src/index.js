import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App"; // Add this import statement

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
